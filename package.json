{
  "name": "shah-alam-local-30",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "python3 -m http.server 4173",
    "build": "rm -rf dist && mkdir -p dist/server dist/client/assets && cp index.html site.html dist/client/ && cp -R assets/. dist/client/assets/ && cp worker/index.js dist/server/index.js"
  }
}
